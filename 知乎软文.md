# 花了两个周末，我用 Pytest + Allure 搭了套 API 自动化测试框架，代码量不到 1500 行

写自动化测试最烦什么？不是写断言，是配环境、串接口、看报告。

一个项目四套环境，配置文件改来改去；登录拿到的 token 要传给下个接口，全靠全局变量硬传；CI 跑挂了去看 Allure 报告，请求体居然没记下来，还得翻日志。

这些痛点逼着我把公司几个项目的测试代码抽象了一下，基于 **Pytest + Allure + Requests** 搭了一个可复用的 API 测试框架。不需要学新 DSL，会写 Pytest 就能上手。

---

## 一眼看懂：一次接口依赖链

```python
class TestOrderFlow:
    @pytest.mark.api
    def test_login(self, request_client, variable_manager):
        resp = request_client.post("/api/login", json_data={
            "username": "admin", "password": "123456"
        })
        assert resp.status_code == 200
        # 自动从响应中提取 token，存入全局变量
        variable_manager.extract_variables(
            {"token": "$.data.token", "uid": "$.data.user_id"},
            resp.json()
        )

    @pytest.mark.api
    def test_create_order(self, request_client):
        # URL、body 里的 $token$、$uid$ 会被自动替换
        resp = request_client.post("/api/orders", json_data={
            "user_id": "$uid$",
            "items": [{"sku": "BOOK-001", "qty": 1}]
        }, headers={"Authorization": "Bearer $token$"})
        assert resp.status_code == 201
```

无需手动维护全局变量、不需要在用例里写 `get_token()` 工具函数，变量管理器在底层透明完成替换。**JSONPath 提取、$var$ 占位符、跨用例共享，三项机制串联起了任意复杂的接口依赖。**

---

## 四行代码新增一套环境

环境切换是我最满意的设计。一个 `setting/` 包，扫描 `*_config.py` 自动发现所有环境类：

```python
# setting/uat_config.py —— 复制粘贴，改三行即可
from setting.base_config import BaseConfig

class UatConfig(BaseConfig):
    CURRENT_ENVIRONMENT = 'uat'
    BASE_URL = 'https://uat-api.example.com'
    DESCRIPTION = 'UAT 环境'

    HTTP_CONFIG = {**BaseConfig.HTTP_CONFIG, 'verify_ssl': False}
```

执行时一行切换：

```bash
uv run python run_tests.py --environment uat
```

dev / test / staging / prod 四套环境各有差异化的默认配置——dev 开 DEBUG 日志关 SSL 校验，prod 关 Allure 防止日志膨胀——**不用每次切环境改一堆参数**。

代码里读配置的方式也极简：

```python
import setting as config
print(config.BASE_URL)          # 自动路由到当前激活的环境
print(config.LOG_CONFIG['log_level'])
```

模块级 `__getattr__` 代理，IDE 自动补全正常工作。

---

## Allure 报告：不是"能用就行"

很多框架到 Allure 这一步就敷衍了——调个 `allure.attach` 把请求体一丢完事。我的做法是让报告一眼就能定位问题：

- **请求卡片**：彩色 HTTP 方法徽章、格式化 JSON 请求体、可折叠的子表显示 Headers 和 Query Params
- **响应卡片**：状态码颜色区分（绿/黄/红）、Body 自动格式化、截断保护（超过 5000 字符截断）
- **cURL 命令**：每个请求自动生成可复制的 cURL，方便开发在本地重现
- **历史趋势**：自动备份/恢复 `history` 目录，每次运行都能看到趋势图

```bash
# 运行测试并生成报告
uv run python run_tests.py --run-and-report
# 或分步：只生成 / 只打开 / 只预览
uv run python run_tests.py --generate-report
uv run python run_tests.py --open-report
```

---

## 不止 HTTP：数据库、通知、日志一体化

真实项目的接口测试不可能只调 HTTP。

**数据库连接池**：一行配置驱动，SQLite / MySQL / PostgreSQL / SQL Server 自动适配驱动，DBUtils 池化：

```python
DATABASE_CONFIG = {'enabled': True, 'type': 'mysql', 'host': '10.0.0.5', ...}
```

用例里直接取 fixture：`def test_verify(self, database): rows = database.query_dict(...)`。

**通知机器人**：DingTalk / WeCom / Feishu 三种 Webhook 已封装，带 HMAC-SHA256 签名和自动重试（复用 HTTP_CONFIG 的重试策略）。跑完流水线自动推测试摘要到群里。

**日志**：loguru + rich 双引擎。控制台彩色分层（INFO/DEBUG/WARN/ERROR），文件按天轮转；Windows 下 `sys.stdout.reconfigure(encoding='utf-8')` + `SetConsoleOutputCP(65001)`，彻底解决 GBK 乱码。

---

## 为什么不直接用 Postman / HttpRunner / 其他轮子

- **Postman**：适合手工调试，但 Newman 跑 CI 时变量管理很痛苦，报告也不够工程化
- **HttpRunner**：功能强但引入了自己的 DSL（YAML/JSON），团队需要额外学习成本；而本框架的用例**就是标准 Pytest**
- **纯自己搭**：Session 复用、重试策略、变量传递、多环境、报告附件，这些细节自己写至少要一两千行，还未必考虑周全

这个框架的定位是做"脚手架"——**把工程细节封装好，用例代码只关心业务逻辑**。你可以直接 clone 下来，改 `BASE_URL`、写用例、跑起来，十分钟内出报告。

---

## 技术栈与项目结构

```
AutoApiTest/
├── setting/          # 自动发现的环境配置（新增环境无需改任何现有代码）
├── common/
│   ├── request_client.py      # HTTP 客户端（Session 复用 + Retry + 变量替换）
│   ├── variable_manager.py    # 全局变量 + JSONPath 提取 + $var$ 占位符
│   ├── database.py            # 连接池（4 种数据库）
│   ├── notification.py        # 通知（邮件 / 钉钉 / 企业微信 / 飞书）
│   ├── allure_attachments.py  # 报告 HTML 附件（请求 / 响应 / cURL）
│   ├── allure_utils.py        # 报告生成 + 历史趋势 + 环境属性
│   ├── runner.py              # pytest 命令构建与子进程执行
│   ├── console.py             # 终端美化（rich）+ 编码统一
│   ├── logger.py              # 日志（loguru）
│   └── tools.py               # 动态变量生成器
├── tests/
│   ├── conftest.py            # 会话级 fixtures
│   └── test_1.py              # 示例用例
├── run_tests.py               # CLI 入口
└── pyproject.toml             # uv 依赖管理
```

**核心依赖**：pytest, requests, allure-pytest, pytest-xdist, loguru, rich, jsonpath-ng, DBUtils，全部通过 uv 管理，`uv sync` 一键安装。

---

## 写在最后

这个框架从实际项目中反复踩坑后抽象而来，目前已在 3 个项目的 CI 流水线中稳定运行。如果你也在维护 API 自动化测试，或者对现有轮子的复杂性感到疲惫，欢迎试一下——clone 下来改个 URL 就能跑，不用看文档。

项目地址：[GitHub - AutoApiTest](https://github.com/se7en8/AutoApiTest)

> 框架采用了 MIT 协议，自由使用、修改、分发。也欢迎提 Issue 或 PR 一起完善。
